import { NextResponse } from 'next/server';

export async function POST() {
  // TODO: Generate content using Claude AI (2-phase: Haiku → Sonnet)
  return NextResponse.json({ data: null, error: null });
}
