// Site Builder - Templates + components + tokens
// Template match → Design tokens → Component assembly → Copy insert → Image placement → Static Export

export async function buildSite(projectId: string, brandDNA: unknown) {
  // TODO: Build static site from brand DNA
  throw new Error('Not implemented');
}
