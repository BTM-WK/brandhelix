// Blog Builder - Markdown → HTML + thumbnail (Satori)

export async function buildBlogPost(content: unknown, brandDNA: unknown) {
  // TODO: Convert content to HTML with thumbnail
  throw new Error('Not implemented');
}
