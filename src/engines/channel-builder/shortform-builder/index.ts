// Shortform Builder - Script + subtitle text + image sequence + thumbnail

export async function buildShortform(content: unknown, brandDNA: unknown) {
  // TODO: Generate shortform package (script + images + thumbnail)
  throw new Error('Not implemented');
}
