// Satori + Sharp pipeline - React → SVG → PNG

export async function generateImage(
  component: unknown,
  width: number,
  height: number
) {
  // TODO: Render React component to PNG via Satori + Sharp
  throw new Error('Not implemented');
}
